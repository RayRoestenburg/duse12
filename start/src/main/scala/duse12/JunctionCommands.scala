package duse12

import akka.actor.Actor
import duse12.messages._
/**
 * TODO add handling of command messages, forward to Queries
 */
class JunctionCommands extends Actor {
  def receive = {
    case _ => {

    }
  }
}